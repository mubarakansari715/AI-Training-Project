# --- UI ---
streamlit>=1.37

# --- Config ---
python-dotenv>=1.0

# --- RAG framework (used selectively: just the recursive text splitter) ---
langchain-text-splitters>=0.2

# --- LLM ---
google-genai>=1.0
requests>=2.31  # OpenRouter fallback (plain REST, no SDK needed)

# --- Embeddings ---
# Pinned for compatibility with torch 2.2.2, the last version PyTorch
# published for Intel (x86_64) macOS.
# CPU-only index avoids pulling bundled CUDA libs on Linux deploy targets
# (e.g. Streamlit Community Cloud); macOS has no CUDA wheels anyway so this
# is a no-op locally.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy<2
torch==2.2.2
transformers==4.44.2
sentence-transformers==3.0.1

# --- Vector store ---
chromadb>=0.5

# --- Document parsing ---
pypdf>=4.2
python-docx>=1.1
markdown>=3.6

# --- Testing ---
pytest>=8.0
pytest-mock>=3.14
